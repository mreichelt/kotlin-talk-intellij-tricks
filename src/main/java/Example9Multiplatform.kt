const val commonGradleFile = "../../../common/build.gradle.kts"
const val platformExpect = "../../../common/src/commonMain/kotlin/Platform.kt"

// Cmd + Click to jump to files
// Multiplatform project
// add macOS / iOS / JS targets
// getPlatformName shows errors for missing implementations
// add src folders for new targets / implementations
// jump between expect / actual
// rename to getPlatformDescription
// add PlatformTest
